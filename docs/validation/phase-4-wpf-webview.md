# 阶段 4：WPF 与 WebView2 产品闭环验收记录

## 完成范围

- 主窗口完成命令栏、内容区、状态栏和全部原生状态视图。
- 工作目录支持选择、结构化更新，并在 DSH 运行期间禁用修改。
- WebView2 使用持久用户数据目录，只加载已确认 DSH 的同源页面，并将外部链接交给系统浏览器。
- 页面刷新与 DSH 生命周期分离；F5、Ctrl+Alt+R、Ctrl+Alt+L 和 F6 进入统一宿主路由。
- 日志窗口显示最近 1,000 行 stdout/stderr，并支持 Esc 关闭。
- WPF 已接入真实 `HarnessLifecycleCoordinator`、健康探测和进程管理链路。

## 自动化结果

| 验证项 | 结果 |
|---|---|
| Release 构建 | 通过，0 警告/0 错误 |
| 单元测试 | 42 项通过，0 失败，0 跳过 |
| 集成测试 | 14 项通过，0 失败，0 跳过 |
| 页面刷新 | F5 后 DSH 仍由原 PID `41428` 监听 `3080` |
| 日志窗口 | 显示最近行、来源、时间和 DSH URL |
| 日志快捷键 | Ctrl+Alt+L 可从宿主控制栏打开日志窗口 |
| 运行中命令状态 | 启动和工作目录选择禁用；停止、重启、刷新可用 |

## 真实 UI 与退出验收

- 1280x820 窗口下命令栏、官方 DSH Web UI 和状态栏无重叠或裁切。
- WebView2 非空渲染 `@deepseek-ai/dsh@0.1.0-rc.6` 官方界面。
- Owned DSH 运行时，主窗口状态明确显示“应用实例运行中”。
- 关闭主窗口后，桌面宿主和 Owned DSH 均退出，`3080` 端口释放。
- 验收期间未操作外部链接，不产生离开已验证 DSH origin 的导航。

## 阶段结论

阶段 4 门禁通过，可以进入阶段 5 的配置持久化、滚动脱敏日志、单实例、退出协调和发布验收。

## 发布缓冲期界面调整

2026-08-15 统一桌面宿主按钮视觉：

- 顶部生命周期、页面刷新和日志操作改为 34x32 图标工具按钮，采用透明常态、浅灰悬停、低透明度禁用态和 5px 圆角。
- 生命周期操作与页面工具之间增加细分隔线；目录、关于及状态页操作复用同一按钮体系。
- 图标使用系统 `Segoe Fluent Icons`，并以 Windows 10 的 `Segoe MDL2 Assets` 回退；所有图标按钮保留中文 Tooltip 和自动化名称。
- 真实 self-contained Release EXE 在 1280x820 下加载官方 DSH Web UI；命令栏、WebView2、状态栏和关于弹窗无重叠或裁切。
- 正常关闭日志保持 `1000 -> 1003 -> 1110 -> 1001`，关闭后桌面进程和 `3080` 监听均为 0。
