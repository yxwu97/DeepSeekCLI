<Window x:Class="DeepSeekHarnessDesktop.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="服务设置"
        Width="570"
        Height="330"
        MinWidth="500"
        MinHeight="300"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource CanvasBrush}">
  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock FontSize="20" FontWeight="SemiBold" Text="本机 DeepSeek Harness 服务" />
    <TextBlock Grid.Row="1" Margin="0,7,0,16" Foreground="{StaticResource MutedTextBrush}" Text="服务地址" />
    <TextBox Grid.Row="2"
             MinHeight="34"
             VerticalContentAlignment="Center"
             Text="{Binding ServiceAddress, UpdateSourceTrigger=PropertyChanged}"
             AutomationProperties.Name="本机服务地址" />
    <TextBlock Grid.Row="3"
               Margin="0,14,0,8"
               TextWrapping="Wrap"
               Foreground="{StaticResource MutedTextBrush}"
               Text="{Binding StatusMessage}" />
    <Grid Grid.Row="4">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal">
        <Button Command="{Binding RestoreDefaultCommand}" Content="恢复默认" />
        <Button Command="{Binding TestConnectionCommand}" Content="测试连接" />
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <Button Command="{Binding CancelCommand}" Content="取消操作" />
        <Button Width="76" Content="关闭" IsCancel="True" />
        <Button Width="88" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ApplyCommand}" Content="应用" IsDefault="True" />
      </StackPanel>
    </Grid>
  </Grid>
</Window>
