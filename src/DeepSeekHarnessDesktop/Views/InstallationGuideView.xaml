<UserControl x:Class="DeepSeekHarnessDesktop.Views.InstallationGuideView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="28">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock FontSize="22" FontWeight="SemiBold" Text="准备 DeepSeek Harness" />
    <TextBlock Grid.Row="1" Margin="0,8,0,18" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Text="{Binding StageMessage}" />
    <Grid Grid.Row="2" Margin="0,0,0,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <StackPanel Margin="0,0,12,0"><TextBlock FontWeight="SemiBold" Text="1  Node.js" /><TextBlock Margin="0,5,0,0" Foreground="{StaticResource MutedTextBrush}" Text="{Binding NodeStatusText}" /></StackPanel>
      <StackPanel Grid.Column="1" Margin="0,0,12,0"><TextBlock FontWeight="SemiBold" Text="2  npx" /><TextBlock Margin="0,5,0,0" Foreground="{StaticResource MutedTextBrush}" Text="{Binding NpxStatusText}" /></StackPanel>
      <StackPanel Grid.Column="2" Margin="0,0,12,0"><TextBlock FontWeight="SemiBold" Text="3  固定版本" /><TextBlock Margin="0,5,0,0" Foreground="{StaticResource MutedTextBrush}" Text="{Binding Diagnostics.DshVersion}" /></StackPanel>
      <StackPanel Grid.Column="3"><TextBlock FontWeight="SemiBold" Text="4  启动" /><TextBlock Margin="0,5,0,0" Foreground="{StaticResource MutedTextBrush}" Text="复用 Owned 进程" /></StackPanel>
    </Grid>
    <Border Grid.Row="3" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="#FAFAFA" Padding="10">
      <ListBox ItemsSource="{Binding RecentLogs}" BorderThickness="0" Background="Transparent" FontFamily="Consolas" FontSize="12" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemTemplate>
          <DataTemplate><TextBlock Text="{Binding Text}" TextWrapping="Wrap" /></DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </Border>
    <Grid Grid.Row="4" Margin="0,16,0,0">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
      <StackPanel Orientation="Horizontal">
        <Button Command="{Binding OpenNodeDownloadCommand}" Content="打开 Node.js 下载页" />
        <Button Command="{Binding RecheckCommand}" Content="重新检查" />
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <Button Command="{Binding CloseCommand}" Content="返回" />
        <Button Command="{Binding CancelCommand}" Content="取消启动" />
        <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding DownloadAndStartCommand}" Content="下载并启动" />
      </StackPanel>
    </Grid>
  </Grid>
</UserControl>
