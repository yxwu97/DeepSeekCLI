# 版本记录

本项目使用语义化版本号 `主版本.次版本.修订号`。每次修改系统代码、配置、构建流程或用户文档后，必须先递增 `Directory.Build.props` 中的 `AppVersion`，并在本文件顶部追加对应版本记录。普通兼容性修改默认递增修订号；新增兼容功能递增次版本号；不兼容变更递增主版本号。

## [0.9.2] - 2026-08-18

### 变更

- 修复已有固定版本 DSH 位于 npx 缓存时仍被判定为缺失、每次提示下载的问题；Auto 现在依次使用全局 DSH、严格校验的现有缓存和带确认的 npx 回退。
- 缓存命中后通过 PATH 中的 Node.js 直接执行固定 DSH 入口，不访问 npm registry；继续使用挂起创建、Job Object、HTTP 身份检查和 Owned 进程树回收。
- 新增缓存包名、版本、bin 映射、错误候选拒绝、解析优先级和免下载确认测试，并同步安装、开发与安全边界说明。

## [0.9.1] - 2026-08-18

### 变更

- 修复 Auto 模式选择 `npx.cmd` 后被挂起进程启动器拒绝并报 `DSH-E103` 的问题；固定的 npx 命令通过系统 `cmd.exe` 受控执行，并继续在恢复运行前加入 Job Object。
- 新增受控 npx 脚本集成测试，覆盖固定参数校验、标准输出、退出码和进程回收。

## [0.9.0] - 2026-08-18

### 变更

- 删除随包 Node/DSH 运行时、manifest/hash Store、Provisioner、回退校验和约 113 MiB sidecar；Auto 恢复为优先使用系统 `dsh.cmd`，否则经用户确认通过 `npx.cmd` 下载并启动固定的 `@deepseek-ai/dsh@0.1.0-rc.6`。
- 启动后依次检查 WebView2、Node.js、npx 和全局 DSH；安装引导的单一主操作按当前缺失项打开官方安装页，环境满足后才准备并启动 DSH。
- 发布改为依赖系统 .NET 8 Desktop Runtime 的 framework-dependent ZIP，不再把 .NET 运行时塞进主 EXE；发布门禁拒绝 Node/npm/DSH 缓存和用户数据，并限制 ZIP 不超过 30 MiB、主 EXE 不超过 5 MiB。
- 保留 Shell 优先显示、Owned Job Object、生命周期串行、立即退出竞态处理、loopback/DSH 身份校验、WebView2 同源限制和日志脱敏；恢复 npm DNS、TLS、registry 与权限错误的稳定分类。

## [0.8.0] - 2026-08-18

### 变更

- 主窗口改为 Shell 优先：单实例与最小设置完成后立即显示，完整依赖诊断移到 `ContentRendered` 后，并与 Code WebView2、DSH lifecycle 并行初始化。
- 托管运行时新增 Fast/Full 两级校验和原子稳定证明。正常复用只读取 manifest、完成标记、关键 Node/DSH 入口及全目录元数据，不再每次读取全部运行时文件正文；首次安装、证明异常、异常退出、修复、回退和变化证据不符仍在进程创建前执行 Full。
- 新增按 runtime id、manifest digest 和策略版本共享的进程内校验协调器，消除诊断与 Provisioner 对同一运行时的重复 Full；等待方取消不取消共享 worker，修复和 key 变化会精确失效结果。
- 7 天周期 Full 在 Usable 后以单 worker 执行。失败时证明立即失效，只有匹配 runtime key 与 generation 的 Owned 进程会经 lifecycle gate 停止，并在一次重建、一次兼容回退预算内恢复；External 和过期结果不受影响。
- 新增稳定启动 EventId、单调计时、结构化验证 I/O 字段、工程专用正常退出 hook 和 `eng/Measure-StartupPerformance.ps1`；正式 Release 门禁拒绝工程 hook，并可执行 20 次 P50/P95 启动阈值检查。
- 托管运行时 Desktop 兼容范围收口为 `>=0.8.0 <0.9.0`，同步更新开发、详细设计、安装和安全限制说明。

## [0.7.4] - 2026-08-18

### 变更

- 根据独立评审修正启动性能方案的方法论：阶段 0 增加最小 Shell 原型和 Fast 微基准，优化后新指标只在原型实测后、生产实现前冻结，不再声称可由旧路径基线直接推出。
- 明确 Fast 是可用性优化而非同用户主动防篡改控制；可信增量证据不可用时显式接受最长 7 天周期 Full 窗口，并要求 ADR 与用户安全说明如实披露。
- 将跨启动 Fast 与同进程 Full 去重、Shell 优先解耦；Fast 证明不可行时仍继续后两项独立优化，并在阶段 0 冻结后台失败的 lifecycle 方法签名级契约。
- 补充 Shell 前服务构造器 I/O 审计、正式 Release 禁用基准控制信号的负向测试、逐交付批次版本递增规则，并将预计工期调整为 15 至 24 个工作日。
- 本版本仍只修订需求与开发计划，尚未实现启动性能功能。

## [0.7.3] - 2026-08-18

### 变更

- 新增 Desktop 启动性能优化需求 Prompt，记录正常启动约 37 至 57 秒的实测分解，确认窗口显示前和生命周期内重复校验约 33,000 个文件是主要瓶颈，DSH 自身启动仅约 2 至 3 秒。
- 新增对应开发计划，冻结 Shell 优先、进程内校验去重、Fast/Full 分层、稳定校验证明、后台完整复核失败闭环、结构化启动计时和 P50/P95 发布门禁。
- 本版本仅形成需求、架构约束和实施计划，尚未修改启动流程或运行时校验实现。

## [0.7.2] - 2026-08-18

### 变更

- 修复已安装托管运行时通过完整性检查，但从不带 sidecar 的开发 Release 输出启动时仍被 `DSH-E216` 阻断的问题；sidecar 缺失时可复用与当前 Desktop 兼容且重新验证通过的活动运行时。
- sidecar 存在时仍优先安装随包运行时，强制重建仍必须提供 sidecar，避免活动运行时复用阻止版本升级或放宽完整性边界。

## [0.7.1] - 2026-08-18

### 变更

- 新增受控的开发启动脚本，自动复用或生成锁定的托管运行时，并将同一 sidecar 暂存到 Debug/Release 开发输出；修复直接执行未携带运行时的开发构建会报 `DSH-E216` 的问题。
- 应用启动日志记录实际基目录和期望的托管运行时路径，便于区分完整发布包、开发输出和被单独复制的 EXE。
- `DSH-E216` 和安装引导明确提示完整解压发布 ZIP，避免从压缩包内直接运行或只复制 EXE 导致 sidecar 缺失。

## [0.7.0] - 2026-08-18

### 变更

- Auto 启动改用随正式包发布的托管运行时，内含私有 Node `24.15.0`、DSH `0.1.0-rc.6` 及精确锁定的生产依赖；用户设备不再执行 npm/npx 或依赖用户缓存、registry 和全局 PATH。
- 新增严格 manifest/hash 校验、安全解包、同卷原子提交、完成标记、活动/上一版本指针和运行租约；损坏时每次操作最多自动重建一次，启动或健康失败时最多回退一次兼容运行时。
- 回退成功后冻结本 Desktop 进程周期内已知失败的运行时，后续启动直接复用已提升的兼容版本；活动指针损坏时可从原子替换留下的备份恢复。
- Auto Owned 进程改为挂起创建、先加入带 `KILL_ON_JOB_CLOSE` 的 Job Object 再恢复执行，关闭创建到归属之间的子进程逃逸窗口；外部实例仍只连接，不停止或重启。
- 安装引导和“关于”窗口改为展示运行时资产、私有 Node、托管 DSH、准备进度和可选外部诊断，不再要求用户手动安装 Node 或运行 npm 命令。
- 发布构建新增精确 lockfile、固定 Node 下载 hash、NOTICE、确定性运行时归档和二次构建比对；正式 ZIP 同时包含 EXE、README、NOTICE 与托管运行时 sidecar，并执行真实版本和 HTTP 身份 smoke。
- 新增 `DSH-E216` 至 `DSH-E220` 托管运行时错误语义；`DSH-E215` 保留给历史迁移诊断，0.7 Auto 不产生该错误码。

## [0.6.2] - 2026-08-17

### 变更

- 新增 DSH 托管运行时与自动修复需求 Prompt，记录新设备在 `Node 24/npm 11` 下把顶层 `rc.6` 的范围型传递依赖解析为 `rc.7` 并停滞于 `idealTree` 的真实根因。
- 新增对应分阶段开发计划，目标版本为 `0.7.0`，覆盖完整依赖锁定、私有 Node + DSH 离线运行时、原子准备、自修复、上一版本回退、供应链审计和干净 Windows 发布门禁。
- 根据独立评审补齐计划一致性与追溯：冻结私有目录结构和 E215 预留语义，增加跨 Desktop 版本回退兼容校验、确定性归档元数据规范化、npx 遗留触点清理及 Job Object 创建窗口验证。
- 本版本只沉淀需求、架构决策和实施计划，尚未替换 `0.6.1` 的客户端 npx 启动代码。

## [0.6.1] - 2026-08-17

### 变更

- 自动 npx 启动固定使用已验证的 `@deepseek-ai/dsh@0.1.0-rc.6`，避免 npm 将可用缓存原地替换为依赖解析异常的 `rc.7` 后长期无法启动。
- 自动启动在创建 npx 进程后明确显示由 Desktop 自动准备并启动 DSH 及最长等待时间，不再将 npm 准备阶段误报为“正在创建 DSH 进程”。
- 修复自动初始化等待 DSH 时执行停止或退出会泄漏取消异常并触发 `APP-E599` 的问题。
- 发布门禁的 WebView2 交互校验增加失败阶段和完整异常诊断，便于区分环境创建、Code、Chat profile 及状态验证故障。
- 新增 DeepSeek Harness Desktop 系统自动升级需求 Prompt，明确自动检查、用户确认安装、签名清单、下载验证、更新器、回滚、进程所有权和隐私边界。
- 新增对应分阶段开发计划，覆盖 GitHub Release 协议、发布签名、配置迁移、暂存与安全解压、事务替换、启动握手、故障注入、WPF 交互和发布门禁；本版本仅沉淀需求与计划，尚未实施功能代码。

## [0.6.0] - 2026-08-17

### 变更

- “DeepSeek 账号”窗口新增“官方充值”入口，在系统默认浏览器打开 DeepSeek 官方充值页面。
- 充值地址使用受控固定 HTTPS 资源，不携带 API Key、余额或其他账户数据。

## [0.5.0] - 2026-08-17

### 变更

- “关于”窗口新增“版本记录”页签，按版本、日期和变更项显示系统历史版本，支持滚动查看。
- 版本记录直接从编译进应用的 `VERSION_HISTORY.md` 读取，离线及 single-file 发布环境无需额外文件。

## [0.4.1] - 2026-08-17

### 变更

- “关于”窗口新增本系统 GitHub 项目入口，方便用户在系统浏览器中查看项目及前往 Releases 下载发布版本。
- 调整“关于”窗口操作区为两行布局，避免新增入口在最小窗口宽度下挤压或溢出。

## [0.4.0] - 2026-08-17

### 变更

- 安装引导新增 desktop/stdout/stderr 详细脱敏日志、阶段与总耗时、1000 行统一缓冲和 5 分钟默认准备期限。
- 新增复制手动命令、复制日志、在工作目录打开 PowerShell及 Node.js、DSH 文档和 npm 包官方快捷入口。
- 自动启动改为无版本受控模板 `npx -y @deepseek-ai/dsh web`，关于窗口的 npm `latest` 查询调整为只读信息，不再比较固定版本。
- 诊断与命令解析每次合并 Machine/User/Process PATH；配置升级到 schema v2，并统一迁移主配置和备份中的历史 60 秒默认值。
- 新增 npm DNS、TLS、registry、权限错误分类以及日志、计时、手动操作、PATH 和配置迁移回归测试。

## [0.3.1] - 2026-08-17

### 变更

- 新增 DSH 安装体验、过程可观测性与版本策略改进需求 Prompt，明确详细脱敏日志、实时耗时、5 分钟准备期限、PATH 刷新、手动安装快捷入口和无固定版本 npx 启动要求。
- 新增对应分阶段开发计划，覆盖统一的主/备份配置迁移、单一日志容量来源、阶段与总耗时、命令安全、资源释放、空白 Windows 用户验证、测试矩阵和发布门禁；本版本仅沉淀需求与计划，尚未实施功能代码。

## [0.3.0] - 2026-08-17

### 变更

- 新增进程内 Code/Chat 模式切换，应用启动默认 Code，Chat 首次选择时懒加载，两个 WebView2 页面实例在托盘隐藏和模式往返期间持续保留。
- 拆分 Code/Chat 页面服务并共享 WebView2 environment；Code 保留默认 profile，Chat 使用固定隔离 profile、独立状态、重试、有限恢复和范围受控的数据清除。
- 新增 Chat 精确 origin 策略、预期导航取消隔离、外链收敛、权限默认拒绝和下载默认取消，并保持 Code 的已确认 loopback DSH 同源边界不变。
- 按当前模式路由刷新、焦点和生命周期命令，补充 22 项模式/URI/错误/profile 单元测试及真实 WebView2 双 profile 验证通道。
- 更新开发、详细设计、安装、维护和发布门禁文档，记录官方登录流程、原生密码提示和多 DPI 环境仍需外部人工验证。

## [0.2.0] - 2026-08-17

### 变更

- 新增可取消、可重试的依赖诊断与安装引导，支持全局 DSH 或 Node.js+npx 两条启动路径，并继续复用 Owned DSH/Job Object 生命周期。
- 新增 loopback 服务地址设置、非持久连接测试、原子保存、外部实例事务切换及 Owned 实例安全换端口重启。
- 新增关于窗口手动 npm `latest` 检查、SemVer prerelease 比较和受限官方链接，不自动下载、安装或切换 DSH 版本。
- 集中维护 DSH 包元数据和服务 URI 安全规则，扩展端口模板、取消竞态、回滚、HTTP/JSON 上限及 ViewModel 测试。
- 固定发布产物的 ProductVersion 为应用语义化版本，避免 SDK 自动附加 Git 提交哈希导致发布门禁失败。

## [0.1.10] - 2026-08-17

### 变更

- 根据评审修订 Code/Chat 开发计划，冻结计划 01 的前置顺序、Code WebView 服务命名以及外链和二次确认接口扩展方案。
- 增加登录跨 origin 三分支安全门禁、原生密码提示环境依赖和专用 Windows WebView2 交互测试门禁，避免将系统浏览器登录误认为应用 profile 登录。

## [0.1.9] - 2026-08-17

### 变更

- 新增 Code/Chat 模式切换分阶段开发计划，明确双 WebView2 页面、Code/Chat profile 隔离、精确导航白名单和生命周期边界。
- 补充 Chat 登录态持久化、原生密码保存、范围受控清除、竞态测试、UI 验证和发布门禁安排。

## [0.1.8] - 2026-08-17

### 变更

- 完善 Code/Chat 模式切换需求 Prompt，明确 Chat 登录会话需要通过稳定、独立的 WebView2 profile 跨应用重启保留。
- 增加 WebView2 原生密码保存授权、登录数据隔离、“清除 Chat 登录信息”、隐私边界及验收测试要求。

## [0.1.7] - 2026-08-16

### 变更

- 新增 Code/Chat 模式切换功能需求 Prompt，明确应用进程启动时默认进入 Code，并可切换到 DeepSeek 官方免费 Chat 页面。
- 明确双页面状态保持、Harness 生命周期隔离、远程导航精确白名单、WebView2 权限和安全验收要求。

## [0.1.6] - 2026-08-16

### 变更

- 新增安装引导、自定义本机服务地址和手动更新检查的分阶段开发计划。
- 计划明确现有组件影响、生命周期与导航安全边界、验收追踪、测试矩阵和发布门禁。

## [0.1.5] - 2026-08-16

### 变更

- 新增 DeepSeek Harness 安装引导、自定义本机服务地址和更新能力的需求 Prompt。
- 基于官方 developer preview、npm 运行方式与 CLI 端口参数完成更新策略评估，明确仅手动检查版本，不做静默升级或未经验证的一键更新。

## [0.1.4] - 2026-08-16

### 变更

- 新增 GitHub 项目说明，明确本项目是 DeepSeek Harness 的 Windows 桌面外壳，提供接近 Codex CLI 与 Claude Code 的便捷使用体验。
- 说明宿主采用 C#、.NET 8、WPF 和 WebView2，并继续复用官方 DeepSeek Harness Web UI 的业务能力。

## [0.1.3] - 2026-08-16

### 变更

- 将发布目录、发布 ZIP 和发布门禁报告统一输出到仓库根目录的 `output/`。
- 更新发布目录清理保护和仓库忽略规则，避免生成物被误提交。

## [0.1.2] - 2026-08-16

### 变更

- 将 AI 维护指南修正为符合 DeepSeek Harness Desktop 的 .NET 8、WPF、WebView2 和 Windows 进程宿主架构。
- 补充进程所有权、生命周期并发、命令转义、服务身份、导航隔离、凭据脱敏、配置兼容及分层测试规则。
- 明确仓库目录职责、事实来源、版本同步流程、发布门禁和交付检查项。

## [0.1.1] - 2026-08-16

### 变更

- 将应用版本集中到 `Directory.Build.props`，供程序集、文件和产品版本共同使用。
- 主窗口版本显示和 DSH 启动环境变量改为读取当前程序集版本。
- 发布与发布验证脚本改为读取当前版本，并在发布前检查本文件是否存在对应记录。
- 建立“每次系统修改必须递增版本并记录变更”的仓库维护规则。

## [0.1.0] - 2026-08-15

### 变更

- 完成 DeepSeek Harness Desktop MVP，包括 DSH 生命周期管理、WebView2 宿主、配置、日志、诊断和发布验证。
